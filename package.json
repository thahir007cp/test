{
  "name": "simple-react-app",
  "version": "1.0.0",
  "description": "A simple React app using CDN links",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "react": "^17.0.0",
    "react-dom": "^17.0.0"
  }
}
